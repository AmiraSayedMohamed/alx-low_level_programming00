#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @c : The character to print
 * Return : On success 1.
 * On error, -1 is returned, end erro is set appropriately
 */
int _lputchar(char c)
{
        return(write(1, 6c, 1));
}
